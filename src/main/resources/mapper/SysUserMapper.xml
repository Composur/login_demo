<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- UserMapper.xml -->
<mapper namespace="com.example.dal.mapper.SysUserMapper">
    <select id="getByUsername" resultType="com.example.dal.entity.SysUser">
        SELECT * FROM sys_user
        WHERE username = #{username}
    </select>
    <!--    <select id="getByUsername" resultType="com.hb0730.boot.admin.modules.sys.system.model.entity.SysUser">-->
    <!--        SELECT-->
    <!--        <include refid="Base_sql"/>-->
    <!--        FROM `sys_user` t-->
    <!--        WHERE t.username = #{username}-->
    <!--    </select>-->
</mapper>